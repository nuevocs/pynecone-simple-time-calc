version: '2'
services:
  pynecone:
    container_name: pycone-working-time-calculator
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    environment:
      - API_URL=${API_URL}
    ports:
      - "${PORTS_PUBLIC}:8000"
      - "${PORTS_API}:3000"